import React from "react";
import { render } from "@testing-library/react";
import TagsForm from "./TagsForm";

test("renders TagsForm", () => {
  render(<TagsForm />);
  expect(false).toBe(true);
});
