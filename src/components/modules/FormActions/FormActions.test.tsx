import React from "react";
import { render } from "@testing-library/react";
import FormActions from "./FormActions";

test("renders FormActions", () => {
  render(<FormActions />);
  expect(false).toBe(true);
});
