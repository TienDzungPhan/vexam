import React from "react";
import { render } from "@testing-library/react";
import Biography from "./Biography";

test("renders Biography", () => {
  render(<Biography />);
  expect(false).toBe(true);
});
